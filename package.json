{
  "name": "tiny-overlay",
  "version": "1.0.0",
  "description": "Overlay for adding help content to TinyMCE",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "devDependencies": {
    "tinymce": "^5.0.3"
  },
  "dependencies": {
    "jquery": "^3.3.1",
    "tingle.js": "^0.14.0"
  }
}
